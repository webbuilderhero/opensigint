# TODO: Add any missing import statements

import signal

# creates instance of the signal class to decode incoming signal
sig = signal.Signal()

# the main decoder function for DPRK-ARQ Keguri
def decode_dprk_arq_keguri(signal): 
    # TODO: decode signal here

    # returns the decoded signal
    return decoded_signal

# takes in the signal and decodes it using DPRK-ARQ Keguri
def decode_input_signal(signal): 
    # calls the decode function for DPRK-ARQ Keguri to decode the input signal
    decoded_signal = decode_dprk_arq_keguri(signal) 

    # returns the decoded signal
    return decoded_signal