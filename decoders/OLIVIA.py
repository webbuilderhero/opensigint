# TODO: Replace variables with actual names

def decoder_OLIVIA(data):
  """Decodes OLIVIA RF signal intelligence

  Args:
    data (bytes, bytearray, or file-like object): Data to decode
  
  Returns:
    decoded (dict): decoded data
  """
  
  # Initialize empty dictionary that holds decoded data
  decoded = {}
  
  # TODO: Decode data
  # Some examples depending on data structure:
  # - If `data` is bytes object: `decoded["payload"] = data.decode("utf-8")`
  # - If `data` is bytearray: `decoded["payload"] = data[3:-1]`
  
  # Return decoded data
  return decoded